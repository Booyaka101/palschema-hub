node_modules/
*.log
.DS_Store
.claude/
# CLI build output is committed so the repo runs after clone without a build step.
# (npm publish rebuilds it via prepublishOnly.)
.cache/

# Rust build output for tools/ooz-decompress (built on demand by npm run extract).
tools/**/target/

# Local build notes, not for the public repo
PROGRESS.md
